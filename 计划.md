# 计划 V2 — 优化后 5 周学习计划（第 4-8 周）

> 基于已完成 3 周基础 + 新需求（MCP/Skills/Multi-Agent/Dify/LangFuse/Computer Use 等）重新编排

---

## 现状盘点：已完成什么

| 需求分类 | 状态 | 对应代码 |
|----------|------|----------|
| **Python + FastAPI** | ✅ 完成 | `app/main.py` — 完整路由、流式输出、文件上传 |
| **RAG 基础 Pipeline** | ✅ 完成 | Loader → Splitter → Embedding → FAISS → Retriever → Reranker |
| **Tool Calling Agent** | ✅ 完成 | `app/agent/agent_executor.py` — 手写 Agent Loop + `ToolRegistry` |
| **多轮对话 Memory** | ✅ 完成 | `app/memory/session_memory.py` — `SessionMemoryManager` + `ConversationMemory` |
| **Docker** | ✅ 完成 | `dockerfile` 已就绪 |
| **LangGraph** | 🔄 进行中 | Day 1 概念学完 (`day1_notes.md`) |
| **LangChain** | ❌ 未开始 | |
| **向量库 (Milvus/Chroma)** | ❌ 仅 FAISS | |
| **Dify / Coze** | ❌ 未开始 | |
| **Agentic-RAG / GraphRAG** | ❌ 未开始 | |
| **MCP / Skills** | ❌ 未开始 | |
| **Multi-Agent** | ❌ 未开始 | |
| **可观测 (LangSmith/LangFuse)** | ❌ 仅基础 logging | |
| **Computer Use** | ❌ 未开始 | |
| **Harness 概念** | ❌ 未开始 | |

---

## 学习优先级总览（适配实习）

> 参考你的五层分类体系重新编排

| 梯队 | 内容 | 时间占比 | 深度要求 | 对应周次 |
|------|------|---------|---------|---------|
| 🥇 **第一梯队** | FastAPI / RAG / LangChain / LangGraph | 50% | 写项目 | 第 4-5 周 |
| 🥈 **第二梯队** | Dify / Coze / 向量库 / LangFuse | 25% | 搭建 + 会用 | 第 5 周 + 第 7 周 |
| 🥉 **第三梯队** | Harness / Multi-Agent / MCP / Skills / Computer Use | 20% | 看懂概念 + 画架构图 | 第 6 周 + 第 8 周 Day 1-2 |
| 🛠️ **辅助** | VibeCoding | 5% | 日常提效 | 贯穿 |

---

## 优化后 5 周计划总览

```
第 4 周：LangGraph 落地 + LangChain LCEL 速通 + 混合检索
         ↓
第 5 周：Agentic-RAG + GraphRAG + 向量库升级 + Dify/Coze
         ↓
第 6 周：MCP/Skills 机制 + Multi-Agent
         ↓
第 7 周：工程化闭环（LangFuse + Harness 概念）
         ↓
第 8 周：Computer Use 前沿 + 求职冲刺
```

---

# 第 4 周：LangGraph 落地 + LangChain LCEL 速通

> 🥇 第一梯队：LangGraph（重点）+ LangChain（速通基础）+ 混合检索

### 前半周

| 天 | 内容 | 详情 |
|----|------|------|
| **Day 1** | LangGraph 核心概念 | State / Node / Edge / Conditional Edge / Checkpoint。对比 `agent_executor.py` | ✅ 已完成 |
| **Day 2** | 手写 Agent 迁移到 LangGraph | 用 `StateGraph` 重写 `agent_executor.py` 的 Tool Calling 循环 |
| **Day 3** | Research Agent：Planner → Search → Writer | 多步推理工作流：Plan 拆解子问题 → Search 逐子问题搜索 → Writer 综合生成 |

### 后半周（新增内容）

| 天 | 内容 | 详情 |
|----|------|------|
| **Day 4** | **LangChain LCEL 速通** | Chain、RunnablePassthrough、RunnableLambda、`\|` 管道符。用 LCEL 把你现有的 RAG Pipeline 重写一遍（文档加载 → 切分 → 检索 → 生成），感受声明式链式调用和你手写过程的区别 |
| **Day 5** | Human-in-the-Loop + Checkpoint | 在 Research Agent 中加入 `interrupt_before` 审批节点 + `MemorySaver` 持久化 |
| **Day 6** | **混合检索 Hybrid Search** | 把你现有的 FAISS 向量检索 + BM25 稀疏检索结合（使用 `rank_bm25` 库），在 Retriever 层做融合排序。这比单纯升级向量库更能体会"为什么需要混合召回" |
| **Day 7** | 周回顾 + README + 面试问答 | |

### 第 4 周面试新增问题

| 问题 | 要点 |
|------|------|
| **LCEL 的 `\|` 管道符做了什么？** | 声明式链式调用，自动处理输入输出传递 |
| **BM25 + 向量检索为什么互补？** | BM25 擅长关键词精确匹配（召回），向量擅长语义相似（泛化），混合后兼顾精度和覆盖面 |
| **LangGraph 相比 ReAct 有什么优势？** | 自定义 DAG 工作流，State 管理灵活，Checkpoint 断点恢复，Human-in-the-Loop |

---

# 第 5 周：Agentic-RAG + GraphRAG + 向量库 + 平台工具

> 🥇 第一梯队：RAG 深度 + 🥈 第二梯队：Milvus / Chroma / Dify / Coze

这是让你的 RAG 项目从"基础可用"升级到"大厂水准"的关键一周。

### 时间安排

| 天 | 内容 | 详情 |
|----|------|------|
| **Day 1** | **Agentic-RAG 概念 + 实现** | RAG 不只是 Retrieve→Generate。Agentic-RAG = Agent 自主决定"是否需要检索、检索什么、检索多少次"。用 LangGraph 改造你的 RAG Agent：加入 Query Rewrite Node（多轮对话中改写模糊问题）+ Self-Reflection Node（回答质量不够时自动重新检索） |
| **Day 2** | **GraphRAG 概念 + 知识图谱构建** | 理解 GraphRAG vs 传统 RAG：传统 RAG 检索孤立 chunk，GraphRAG 检索关联实体和关系。用 NetworkX 构建简单知识图谱（从 `employee_policy.txt` 提取实体），实现基于图遍历的增强检索 |
| **Day 3** | **Milvus / Chroma 向量库实战** | 不要只用 FAISS。先后搭建 Chroma（轻量，本地开发首选）和 Milvus（分布式，生产首选），把你的 RAG Pipeline 的 VectorStore 抽象层对接这两种向量库 |
| **Day 4** | **Reranker 模型升级** | 替换现有字符重合度 Reranker 为 Cross-Encoder 模型（如 `BAAI/bge-reranker-v2-m3`），感受精排效果的质变 |
| **Day 5** | **Dify 实战** | 用 Dify 搭建一个和你的 RAG Agent 功能等价的知识库问答应用（上传文档 → 向量化 → 对话）。目标：理解低代码平台的抽象层次——面试时能说"我既能手写 RAG Pipeline，也能用 Dify 快速验证想法" |
| **Day 6** | **Coze 扣子实战** | 用 Coze 搭建一个 Multi-Agent 工作流（如：客服机器人 = 意图识别 Agent + 知识库 Agent + 转人工 Agent）。理解 Bot、Plugin、Workflow、Knowledge 四个核心概念 |
| **Day 7** | 周回顾 + Dify vs Coze vs 手写代码 三角对比表 | |

### 第 5 周面试新增问题

| 问题 | 要点 |
|------|------|
| **Agentic-RAG 和传统 RAG 区别？** | 传统 RAG 固定 Retrieve→Generate；Agentic-RAG 由 Agent 自主决策检索时机、次数、策略 |
| **GraphRAG 解决什么问题？** | 传统 RAG 丢失实体间关系，GraphRAG 保留知识图谱结构，回答多跳问题 |
| **为什么选 Milvus 而不是 FAISS？** | FAISS 是内存索引（无持久化/分布式），Milvus 支持分布式、持久化、混合检索 |
| **Dify 和手写 RAG 各自适用场景？** | Dify 适合快速验证/内部 Demo；手写代码适合深度定制/高并发生产系统 |
| **Chroma vs FAISS vs Milvus？** | Chroma 轻量 + 自带 metadata 过滤，FAISS 纯向量索引速度快，Milvus 企业级分布式 |

---

# 第 6 周：MCP/Skills + Multi-Agent

> 🥉 第三梯队：MCP/Skills、Multi-Agent — 面试区分度的关键

### 时间安排

| 天 | 内容 | 详情 |
|----|------|------|
| **Day 1** | **MCP 协议核心概念** | 理解 MCP 架构：MCP Host（Agent）→ MCP Client → MCP Server（工具提供方）。核心理解：你现在的 `ToolRegistry` + `SearchTool` 是"工具硬编码在 Agent 内部"，MCP 把它变成"工具作为独立服务运行"。类比：手写 SQL vs ORM |
| **Day 2** | **MCP Server 实战** | 把你现有的 `knowledge_search` 工具封装为一个独立 MCP Server（用 `mcp` Python SDK）。然后让你的 LangGraph Agent 通过 MCP Client 调用它。**这一步让你真正理解"工具标准化"** |
| **Day 3** | **Skills 机制** | 理解 Skills = Agent 能力的模块化封装（类似微服务中的"可插拔能力模块"）。对比你现有的 `app/agent/tools.py`（单文件硬编码）和 Skills 的"注册→发现→调用"模式。设计一个 Skill 注册协议 |
| **Day 4** | **Multi-Agent 架构设计** | 理解 Multi-Agent 三种模式：① 顺序流水线（A→B→C）② 路由器（根据意图分发）③ 辩论/协作（多个 Agent 并行输出后综合）。用 LangGraph SubGraph 实现双 Agent 协作：Research Agent（搜索）+ RAG Agent（查知识库），由 Router Agent 分发 |
| **Day 5** | **Multi-Agent 实战：Supervisor 模式** | 实现 Supervisor Agent 模式：Supervisor 接收用户问题 → 决定调用哪个子 Agent（Research / RAG / Code）→ 收集结果 → 综合输出。这是大厂最常用的 Multi-Agent 架构 |
| **Day 6** | **Dify + Coze 回顾** | 用 Dify/Coze 分别搭建 Day 4-5 手写的 Multi-Agent 工作流，对比低代码 vs 手写的开发效率差异 |
| **Day 7** | 周回顾 + "Agent 工具演化史"总结 | 输出文档：硬编码 Tool → MCP 标准化 → Skills 模块化 → Multi-Agent 协作 的演化路径 |

### 第 6 周面试新增问题

| 问题 | 要点 |
|------|------|
| **什么是 MCP？解决了什么问题？** | Model Context Protocol，标准化 Agent-工具通信协议，解耦工具和 Agent |
| **Tool Calling vs MCP？** | Tool Calling 是 LLM 层面（OpenAI Function Calling），MCP 是架构层面（工具服务标准协议），两者互补 |
| **Skills 和 Tools 的区别？** | Tool 是原子操作（搜索、计算），Skill 是能力模块（包含多个 Tool + 内部逻辑 + 状态） |
| **Multi-Agent 的 Supervisor 模式怎么工作？** | Supervisor 作为中央协调者，接收任务 → 分发给子 Agent → 收集结果 → 决策是否需要迭代 |
| **为什么需要 Multi-Agent？** | 单 Agent 处理复杂任务时上下文窗口不够、推理能力受限；Multi-Agent 分工协作，各司其职 |

---

# 第 7 周：工程化闭环

> 🥈 第二梯队：LangFuse 可观测 + 🥉 第三梯队：Harness 概念

### 时间安排

| 天 | 内容 | 详情 |
|----|------|------|
| **Day 1** | **LangFuse 接入** | 给你的 LangGraph Agent 接入 LangFuse 追踪（开源免费，类比 LangSmith）。追踪每次调用的：LLM token 消耗、工具调用耗时、检索耗时。**面试必问："你怎么监控 Agent 性能？"** |
| **Day 2** | **LangFuse 高级** | 设置 Evaluation：自动评估 RAG 回答质量（Faithfulness / Answer Relevance / Context Precision），建立 feedback loop |
| **Day 3** | **Harness 概念体系** | 理解 Harness（智能体驾驭工程）完整框架：Memory 管理（你已有）、权限沙箱（安全边界）、任务调度（优先级队列）、异常重试（Exponential Backoff）。**不需要全部实现代码，但必须能用白板画出架构图** |
| **Day 4** | **异常重试 + 任务调度实战** | 在你的 Agent 中加入：① LLM 调用失败自动重试（exponential backoff）② 多用户请求的并发任务队列（`asyncio.Queue`）③ 超时熔断（超过 30s 返回降级回答） |
| **Day 5** | **权限沙箱概念** | 理解 Agent 安全三原则：① 工具权限最小化 ② 用户审批门（Human-in-the-Loop 你已有）③ 沙箱隔离（Code Interpreter 在 Docker 容器中运行） |
| **Day 6** | **项目整合** | 把第 4-7 周所有改进整合到你的企业 RAG Agent 项目中：LangGraph 工作流 + Agentic-RAG + MCP 工具 + LangFuse 监控 |
| **Day 7** | 周回顾 + 完整架构图绘制 | 画出"企业级 Agent 系统架构图"（FastAPI → LangGraph → MCP → Milvus → LangFuse 全链路） |

### 第 7 周面试新增问题

| 问题 | 要点 |
|------|------|
| **你怎么监控 Agent 性能？** | LangFuse 追踪调用链路（token 消耗、延迟、工具调用次数），设置自动评估指标 |
| **Agent 工程中 Harness 做什么？** | 智能体驾驭层：Memory 管理、权限沙箱、任务调度、异常重试——Agent 的"基础设施" |
| **怎么降低 LLM 调用成本？** | 模型路由（简单任务用小模型）、缓存（相同 query 不重复调 LLM）、embedding 缓存、减少 context |

---

# 第 8 周：前沿方向 + 求职冲刺

> 🥉 第三梯队：Computer Use（GUI 智能体）+ 简历/面试

### 时间安排

| 天 | 内容 | 详情 |
|----|------|------|
| **Day 1** | **Computer Use 概念** | 理解 GUI Agent 的工作原理：截图→视觉模型理解界面→输出操作指令（点击/输入/滚动）→执行。了解 Claude Computer Use、OpenAI Operator、UFO 等项目。**不需要实现代码，理解原理和局限即可** |
| **Day 2** | **Computer Use 前沿论文速览** | 浏览 OS-Copilot、UFO、CogAgent 等论文的摘要和架构图，理解"视觉理解 + 动作空间 + 记忆"三要素 |
| **Day 3** | **简历包装** | 把 8 周所有项目写入简历：① 企业级 RAG Agent（RAG + Agentic-RAG + GraphRAG）② Research Agent（LangGraph + Multi-Agent）③ MCP 工具标准化实践 |
| **Day 4** | **GitHub README 精修** | 每个项目的 README 包含：架构图（mermaid）、技术栈、核心难点、优化方案、Demo 截图 |
| **Day 5** | **面试模拟 Day 1** | RAG 专题（chunk / embedding / rerank / hybrid search / Agentic-RAG / GraphRAG） |
| **Day 6** | **面试模拟 Day 2** | Agent 专题（LangGraph / MCP / Multi-Agent / Harness / 可观测） |
| **Day 7** | **面试模拟 Day 3** | 系统设计："设计一个企业级 RAG 系统"全链路回答 |

### 第 8 周面试新增问题

| 问题 | 要点 |
|------|------|
| **Computer Use 的核心挑战？** | 视觉理解准确率、动作空间设计、安全性（Agent 操控桌面风险） |
| **Agent 是什么？** | LLM + planning + tool + memory + environment（五要素） |
| **设计一个企业级 RAG 系统** | 全链路：文档解析 → 智能分块 → 混合检索 → Reranker → Agentic-RAG → 多轮记忆 → 监控评估 → 权限安全 |

---

## 最终技术栈（简历呈现）

```
Languages:
Python

Backend:
FastAPI / Docker / Linux

LLM:
OpenAI API / DeepSeek API

Agent:
LangGraph / LangChain / Function Calling

RAG:
Agentic-RAG / GraphRAG / FAISS / Milvus / Chroma
Embedding (BGE) / Reranker (Cross-Encoder)

Tools & Platform:
Dify / Coze / MCP / LangFuse

Engineering:
Multi-Agent / Harness / Checkpoint / Human-in-the-Loop

Frontier:
Computer Use (GUI Agent)
```

---

## MCP/Skills 学习时间确认

根据你的需求，**MCP/Skills 具体安排在第 6 周 Day 1-3**：

```
第 6 周 Day 1：MCP 协议概念（Host/Client/Server 三层架构）
第 6 周 Day 2：MCP Server 实战（knowledge_search → MCP Server）
第 6 周 Day 3：Skills 机制概念 + 演化路径设计
```

---

## 与原计划对比

| 维度 | 原计划 (计划.md) | 新计划 (计划_v2.md) |
|------|-----------------|-------------------|
| 周数 | 8 周（第 1-8 周） | 5 周（第 4-8 周，前 3 周已完成） |
| LangGraph | 第 4 周 7 天 | 第 4 周 5 天（减少 2 天，压缩重复内容） |
| LangChain | 未覆盖 | 第 4 周 Day 4（LCEL 速通） |
| RAG 深度 | 无 | 第 5 周（Agentic-RAG + GraphRAG + 混合检索 + Reranker 升级） |
| 向量库 | 无 | 第 5 周 Day 3（Chroma + Milvus 双库实战） |
| Dify / Coze | 未覆盖 | 第 5 周 Day 5-6（低代码工具实战） |
| MCP / Skills | 未覆盖 | **第 6 周 Day 1-3（新增）** |
| Multi-Agent | 未覆盖 | 第 6 周 Day 4-5（Router + Supervisor 模式） |
| LangFuse | LangSmith 提及 | 第 7 周 Day 1-2（LangFuse 实战接入） |
| Harness | 未覆盖 | 第 7 周 Day 3-5（概念 + 异常重试/任务调度实战） |
| Computer Use | 未覆盖 | 第 8 周 Day 1-2（概念 + 论文速览） |

---

> **核心思路：** 前 3 周已打下坚实工程基础，接下来 5 周从"能跑"升级到"能面试"——每个方向都要求能画架构图、能讲设计决策、能对比技术选型。
