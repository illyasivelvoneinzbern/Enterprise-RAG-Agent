# Day 1 笔记：Computer Use / GUI Agent 核心概念

> 目标：理解 GUI Agent（计算机使用智能体）——让 LLM 直接"操作电脑界面"，
> 把前 7 周"文本工具调用"的 Agent 认知升级到"屏幕操作"，
> 并用你已会的 **MCP / ConversationMemory / LangGraph Checkpoint / Harness** 反刍新概念。
>
> 本文档只讲概念，不写业务代码（Day 1 走 Ask 模式，环境受限 + 时间留给求职）。

---

## 一、Computer Use / GUI Agent 是什么？解决什么问题？

**Computer Use（GUI Agent）** 让 LLM 直接"操作电脑界面"：像人一样**看屏幕、点按钮、打字、滚动**，不需要软件提供 API。

**解决的核心问题**：Agent 之前的动作对象是"文本工具"（只能调 API），面对**没有 API 的软件**（旧系统、桌面应用、网页）就无能为力。Computer Use 把动作对象换成"屏幕本身"，于是**任何能看能点的软件都能被 Agent 操作**。

**和你前 7 周 Agent 的差异**（关键）：

| 维度 | 前 7 周的 Agent（你已会） | Computer Use |
|---|---|---|
| 动作对象 | 文本工具：`{"name": "knowledge_search", "arguments": {...}}` | 电脑屏幕：`click(x,y)` / `type(text)` / `scroll` |
| 调用入口 | [`ToolExecutor.execute()`](app/agent/executor.py:13)（MCP `tools/call`） | 操作系统级执行器（真实点击/键入） |
| 输入感知 | 文本 token | 屏幕截图（像素） |
| 一句话 | Agent 看不见界面，只看得见文本参数 | Agent 直接面对界面，像人一样操作 |

### 工作闭环（Day 1 核心图）

```
┌─────────────────────────────────────────────────────────────┐
│  ① 截图 / 屏幕像素流   (把界面变成图像输入)                    │
│        │                                                     │
│        ▼                                                     │
│  ② 视觉模型理解界面 → "这里是'查询'按钮、这里是输入框、        │
│        │              这里有个列表"  (像素 → 界面语义)         │
│        ▼                                                     │
│  ③ LLM 输出操作指令 → 点击(坐标) / 输入(文本) / 滚动 / 按键    │
│        │             （= Agent 决定"下一步调哪个工具"）        │
│        ▼                                                     │
│  ④ 执行器执行操作   (真实点击/键入/滚动到操作系统)             │
│        │                                                     │
│        └────────────→ ① 再截图，看界面是否变化 → 继续          │
│              (看 → 想 → 动 → 再看，直到任务完成)               │
└─────────────────────────────────────────────────────────────┘
```

**与你的 LangGraph Agent 循环对照**：这个闭环 = 你熟悉的 `llm_node → tool_node → 回到 llm_node`：
- `tool_node` 的输入从"工具返回文本"变成"一张新截图"；
- Agent 的决定从"调用哪个工具 + 文本参数"变成"在哪个坐标做什么屏幕操作"。

> **一句话：Computer Use = 把"工具"从 API 换成"屏幕"，Agent 循环的结构没变。**

---

## 二、三要素对照表（关键：用你已会的知识反刍）

| GUI Agent 要素 | 说明 | 与你已有知识的关联 |
|---|---|---|
| **① 视觉理解** | 多模态模型把"像素"变成"界面语义"：认出按钮/输入框/列表/菜单，并给出**屏幕坐标** | 类比你的 **embedding**（[`app/rag/embedding.py`](app/rag/embedding.py:1)）——embedding 把**文本→向量**，视觉模型把**像素→界面语义**。都是"把机器看不懂的原始输入变成可推理的结构化表示"；区别是 embedding 产出向量做相似度检索，视觉模型产出"语义 + 坐标"做操作定位 |
| **② 动作空间** | 定义 Agent 能做的**原子操作集合**：`click(x,y)` / `type(text)` / `scroll(dir)` / `press(key)` / `move(x,y)` | 类比你的 **MCP Tool Schema**（[`app/agent/tool_schema.py`](app/agent/tool_schema.py:1)）——**动作空间 = 工具的另一种形态**。schema 定义"能调哪些工具 + 参数结构"，动作空间定义"能在屏幕上做哪些原子操作 + 坐标/文本参数"；两者都在回答同一问题："Agent 被允许做什么" |
| **③ 记忆** | 跨步骤记住"点过哪里、进行到哪一步、当前在第几页"（截图历史 + 操作轨迹 + 任务状态） | 类比你的 [`ConversationMemory`](app/memory/memory.py:1)（多轮对话历史）+ **LangGraph Checkpoint**（状态跨步骤持久化）。GUI 操作强依赖状态：没记忆，Agent 每步都像失忆，无法完成"填表单 → 提交 → 等结果"的多步任务 |

> **Harness 视角补刀**：三要素只是"能做"。Week 7 你学的 Harness（可靠性/安全）才是落地难点——
> 操作可能点错 → 需要 **self-correct 循环**（看结果对不对、错则回退重试）；
> Agent 在真实桌面 → 需要 **安全边界**（沙箱 / 限权 / HITL 审批门，你在 [`research_agent_hitl.py`](app/agent/research_agent_hitl.py:1) 做过）。

---

## 三、三个代表项目各一句定位

| 项目 | 出品方 | 一句话定位 |
|---|---|---|
| **Claude Computer Use** | Anthropic | 首个把"视觉理解 + 屏幕操作"做成可用产品的 API：Claude 直接接收截图、输出鼠标/键盘动作，让模型"像人一样用电脑" |
| **OpenAI Operator** | OpenAI | 基于 **CUA（Computer-Using Agent）** 的浏览器 Agent，靠"截图 + 动作"闭环自动完成预订/购物/填表等网页任务，主打云端沙箱里的安全托管操作 |
| **UFO** | 微软 | 面向 **Windows 桌面应用**的双智能体框架：`AppAgent`（决策：下一步操作哪个 UI 元素）+ `ActionAgent`（执行：生成具体 UI 动作），不靠 OCR/坐标而是**直接与 UI 元素交互** |

> **Multi-Agent 知识向上兼容**：UFO 的 `AppAgent + ActionAgent` 分工 = 你的 [`supervisor_agent.py`](app/agent/supervisor_agent.py:1) / Router 的"**决策 Agent 分发 + 执行 Agent 干活**"。GUI Agent 也复用 Multi-Agent 思想（Day 2 论文速览展开）。

---

## 四、局限清单（面试关键）

| 局限 | 说明 | 用你已有知识理解为什么难 |
|---|---|---|
| **视觉理解准确率有限** | 认错按钮/边界/元素，界面复杂或动态时更明显 | 类比 embedding：语义理解总有误差，且这里误差直接导致"点错地方" |
| **操作不可靠** | 点击没生效 / 弹窗遮挡 / 异步加载 → 需 **self-correct 循环** | 类比 Week 7 Harness：要重试 / 校验 / 回退，不能"点一下就算成功" |
| **安全风险高** | 在**真实桌面**上操作，可能误删文件、误点危险操作、泄露敏感信息 | 你已有安全视角：必须沙箱 + 权限边界 + HITL 审批门（[`research_agent_hitl.py`](app/agent/research_agent_hitl.py:1)） |
| **执行速度慢** | 每步"截图 → 推理 → 操作"，多步任务明显慢于 API 调用 | 你的 `tools/call` 毫秒级；GUI 一步可能秒级，几十步累积很慢 |

---

## 五、连线题解析：MCP 工具调用 vs GUI 动作空间

**相同点（核心）**：两者都在**定义"Agent 能做什么"**——给 LLM 一份"能力清单"，让它推理后输出一个动作。

| 维度 | MCP 工具调用（你已会） | GUI 动作空间（Computer Use） |
|---|---|---|
| 定义能力的方式 | 工具名 + `input_schema`（JSON 参数结构，见 [`tool_schema.py`](app/agent/tool_schema.py:1)） | 原子操作集：`click(x,y)` / `type(text)` / `scroll` / `press` |
| 动作的参数类型 | **文本/结构化参数**（`query: "年假几天"`） | **屏幕坐标 / 像素 / 按键**（`click(x=340, y=520)`） |
| 执行方 | MCP Server / 本地工具（有明确 API 边界） | 操作系统级执行器（直接操控真实 GUI） |
| 可靠性 | 高（参数校验 + 明确返回） | 低（界面不确定性，需 self-correct） |
| 抽象层级 | "我要查知识库"（**语义级**，不关心怎么查） | "我要点这个按钮"（**像素级**，直接面对界面） |

**连线题答案**：`click(x,y)` 之于 GUI Agent ≈ `tools/call` 之于 MCP Agent——**都是"LLM 选择的动作被系统执行"**；区别是参数是**坐标/像素（屏幕操作）**还是**文本 schema（工具调用）**。甚至可理解成：**GUI 动作空间 = 一个"屏幕版"的 MCP Server**——它暴露的不是 `knowledge_search` 而是 `click / type / scroll`。

---

## 六、面试话术模板

> "Computer Use 本质是把 Agent 的**动作空间**从'文本工具调用'扩展到'屏幕操作'，三要素是**视觉理解、动作空间、记忆**。我之前做 MCP 工具标准化时，动作空间是 `name + input_schema` 的结构化参数；Computer Use 里动作空间变成了 `click/type/scroll` 这样的屏幕原子操作，参数从文本变成了坐标。它最大的挑战是**可靠性**——需要 self-correct 循环和安全边界，这和我做的 **Harness** 是一脉相承的。"

备用短句（回答"你怎么看 Computer Use 这类前沿方向"）：

> "它本质是**把 Agent 的动作空间从文本工具调用扩展到屏幕操作**，三要素是视觉理解、动作空间、记忆。和我们做的 MCP/Multi-Agent 是一套思想：**动作空间定义 Agent 能做什么，Multi-Agent 负责决策与执行分工，Harness 保证可靠与安全**。"

---

## 七、Day 1 任务自检

- [x] 画出 GUI Agent 工作闭环图（截图 → 理解 → 操作 → 再截图）
- [x] 讲清三要素：视觉理解 / 动作空间 / 记忆（并对照 embedding / MCP Tool Schema / ConversationMemory + Checkpoint）
- [x] 说出三个代表项目各一句定位：Claude Computer Use / OpenAI Operator / UFO
- [x] 列举局限清单（面试关键）：视觉准确率 / 操作不可靠 / 安全风险 / 执行速度
- [x] 回答连线题：MCP 工具调用 vs GUI 动作空间——**都定义"Agent 能做什么"，区别是文本参数 vs 屏幕坐标/像素**

> 下一步（Day 2）：用三篇论文（OS-Copilot / UFO / CogAgent）反刍三要素的工程化细节，
> 重点对比 **UFO 双 Agent 分工** 与你的 Supervisor/Router——你会发现 **Multi-Agent 思想是通用的**。

---

# Day 2 笔记：Computer Use 前沿论文速览

> 目标：用三篇论文/项目（**OS-Copilot / UFO / CogAgent**）补全 Day 1 三要素的**工程化细节**。
> **不用细读公式，看懂"架构图 + 三要素 + 创新点"即可。**
> 这一天的真正价值不是"了解 GUI"，而是**用前沿论文反刍你已经会的 Multi-Agent / Skill / Harness 概念**——面试时能"向上兼容"聊前沿。
>
> 本文档只讲概念，不写业务代码（Day 2 走 Ask 模式）。

---

## 一、三篇论文速览表（面试 30 秒版）

| 论文/项目 | 出品方 | 一句话定位 | 对应三要素 | 与你的已有代码关联 |
|-----------|--------|-----------|-----------|-------------------|
| **OS-Copilot（FRIDAY）** | CMU 等 | 把**操作系统变成可交互环境**，含自建**能力库**（generalist→specialist 演化），做完任务会把经验沉淀成新能力 | **动作空间 + 记忆（能力库）** | 你的 [`skill.py`](app/agent/skill.py:1) 的"注册→发现→调用"已是能力库雏形；FRIDAY 多了"从任务自举新 Skill 写回库"的闭环 |
| **UFO** | 微软 | 聚焦 **Windows 应用**操作，**双智能体**：`AppAgent`（决策：下一步操作哪个 UI 元素）+ `ActionAgent`（执行：生成具体 UI 动作），不靠 OCR/坐标而是**直接与 UI 元素交互** | **视觉理解 + 多 Agent 分工** | 你的 [`supervisor_agent.py`](app/agent/supervisor_agent.py:1)（决策分发 + 子 Agent 干活）——**Multi-Agent 思想完全通用** |
| **CogAgent** | 清华 GLM | **180 亿参数视觉语言模型**（VLM），能理解 GUI 截图并**直接定位点击坐标**，把"看懂屏幕"从外挂工具变成模型原生能力 | **视觉理解（模型侧）** | 你的 embedding（[`embedding.py`](app/rag/embedding.py:1)）/ rerank ——都是"**感知层升级**"：原始输入 → 可推理的结构化表示 |

> **一句话记忆**：OS-Copilot 强在"**记忆/能力演化**"，UFO 强在"**决策与执行分工**"，CogAgent 强在"**视觉模型本身**"。三者正好覆盖 Day 1 三要素的工程化落点。

---

## 二、OS-Copilot（FRIDAY）——把操作系统变成可交互环境

### ① 解决了什么问题
通用大模型只能"聊天"，无法像人一样在真实电脑上完成**跨应用任务**（查资料 → 填表格 → 发邮件）。FRIDAY 把**整个操作系统当作 Agent 的可交互环境**：Agent 通过"观察屏幕 + 执行操作"完成任务，并在此过程中**沉淀自己的能力**——从只会简单操作（generalist），进化成特定任务的"熟练工"（specialist）。

### ② 架构图（ASCII）

```
┌────────────────────────  OS-Copilot / FRIDAY  ─────────────────────────┐
│                                                                       │
│   用户任务（口语化：帮我整理报告并导出 PDF）                                │
│        │                                                              │
│        ▼                                                              │
│  ┌─────────────────────────────────────────────┐                      │
│  │        FRIDAY 核心控制器（generalist 大脑）    │                      │
│  │   记忆 / 推理 / 规划 / 经验抽取               │                      │
│  └───────┬─────────────────────────┬───────────┘                      │
│          ▼                         ▼                                  │
│  ┌────────────┐            ┌──────────────────┐                       │
│  │  观察模块   │            │  能力库(记忆)      │ ← 自建"技能清单"        │
│  │  截图/感知  │            │  capability_lib   │                       │
│  │  OS 状态   │            │  通用能力          │                       │
│  └────────────┘            │  专用能力(specialist)│                     │
│          │                 └─────────┬────────┘                       │
│          ▼                           │ 注册新能力(经验萃取)             │
│  ┌────────────┐            ┌─────────▼─────────┐                      │
│  │  执行模块   │◄───────────│   动作规划/执行     │                      │
│  │ 应用/终端  │             │  click/type/execute │                     │
│  │ 真实 OS   │             └───────────────────┘                      │
│  └────────────┘                                                        │
│                                                                       │
│  核心闭环：感知 → 决策 → 执行 → 复盘抽取经验 → 写回能力库 → 下次更快        │
└───────────────────────────────────────────────────────────────────────┘
```

### ③ 三要素如何体现
- **动作空间**：把 OS 动作（点击/键入/启动应用/跑脚本）统一成可执行原语，Agent 通过"能力库"选择最合适的动作。
- **记忆（能力库，最大亮点）**：FRIDAY 从完成的任务中**抽取经验存进能力库**，把通用能力（generalist）逐步提炼成专用能力（specialist）。第一次做"导出 PDF"很费力，几次后就把流程固化成 `export_pdf` 专用能力，下次直接调用。
- **视觉理解**：依赖截图感知（相对弱项，正好由 UFO/CogAgent 补齐）。

### ④ 与你的 [`skill.py`](app/agent/skill.py:1) 的连接（面试可用）
你的 Skill 机制已经是"能力库"的雏形：
- **注册** = 把 `RAGSkill` 按 `name` 放入 SkillRegistry；
- **发现** = `SkillRegistry.discover(query)` / `RAGSkill.can_handle(query)` 用 query 匹配能力；
- **调用** = `execute(state)`。
FRIDAY 比你多的是"**自举闭环**"：完成任务后自动抽取经验 → 生成/更新一个 Skill → 写回库。**面试话术：我的 Skill 覆盖了'注册→发现→调用'，FRIDAY 展示了这条链路的前沿进化方向——能力自我沉淀。**

---

## 三、UFO（微软）——Windows 应用操作的双智能体（本日核心图）

### ① 解决了什么问题
操作 Windows 桌面应用时，OCR + 硬编码坐标的方案很脆弱（窗口一动就点错）。UFO 提出**双智能体分工** + **直接与 UI 元素交互**（拿元素句柄/控件，而不是死记坐标），让"想做什么"与"怎么操作"解耦。

### ② 架构图（ASCII）——核心图

```
┌────────────────────────────  UFO (Microsoft)  ─────────────────────────────┐
│                                                                            │
│   用户指令: "在 Word 里把标题设为红色加粗"                                     │
│        │                                                                   │
│        ▼                                                                   │
│  ┌───────────────────────────────┐                                         │
│  │   HostAgent(宿主/感知)          │                                         │
│  │   读屏幕 + 感知应用状态          │                                         │
│  └────────────┬──────────────────┘                                         │
│               ▼ 截图 / UI 元素树（视觉理解）                                 │
│  ┌────────────────────────────────────────┐                                │
│  │      AppAgent（决策 Agent）               │  ← 像你的 Supervisor/Router    │
│  │   "下一步：选择 '字体' 菜单"               │                                │
│  │   从 UI 元素列表里挑操作目标              │                                │
│  └──────────────┬─────────────────────────┘                                │
│                 ▼ 决策（目标元素 + 动作意图）                                 │
│  ┌────────────────────────────────────────┐                                │
│  │      ActionAgent（执行 Agent）            │  ← 像你的执行工具/Executor       │
│  │   "把动作翻译成: click(元素#id=12)"        │                                │
│  │   生成具体 UI 操作并执行                  │                                │
│  └──────────────┬─────────────────────────┘                                │
│                 ▼ 执行                                                      │
│  真实 Windows 应用（Word/Excel/Outlook...）                                  │
│                 │                                                          │
│                 └── 执行结果 / 新截图 → 回到 HostAgent/AppAgent 复盘 ─────────┘
└────────────────────────────────────────────────────────────────────────────┘
```

### ③ 三要素如何体现
- **视觉理解**：HostAgent 把屏幕转成"**UI 元素树**"（元素 + 语义标签），而非纯像素 OCR——这是它比坐标方案稳的关键。
- **多 Agent 分工（核心创新）**：`AppAgent` = 决策者（"操作谁"），`ActionAgent` = 执行者（"怎么操作"）。**和你的 [`supervisor_agent.py`](app/agent/supervisor_agent.py:1) 完全同构**。
- **记忆**：AppAgent 维护任务状态/已做步骤（决定是否继续）。

### ④ 与你的 Supervisor/Router 的连接（Day 2 最重要的连接点）
| UFO | 你的代码 | 对应关系 |
|-----|---------|---------|
| AppAgent（决策下一步操作哪个 UI 元素） | [`supervisor_node`](app/agent/supervisor_agent.py:147)（LLM 决策派 research/rag/finish） | 都是"**决策 Agent：只回答做什么/派谁**" |
| ActionAgent（生成具体动作并执行） | `rag_node` / `research_node`（子 Agent 干活） | 都是"**执行 Agent：只回答怎么做**" |
| HostAgent（感知应用状态） | 你的 LLM 输入（query/messages） | 都是"**感知层：把环境状态喂给决策者**" |
| 决策→执行→新截图→再决策 循环 | [`supervisor_agent.py`](app/agent/supervisor_agent.py:42) 的 `conditional edge` 回 supervisor | 都是"**可迭代分发 + 收集 + 综合**"的循环 |

> **核心结论：Multi-Agent 思想是通用的。** 你把"执行对象"从"调工具"换成"操作 UI 元素"，Supervisor 模式就变成了 UFO。GUI Agent 没有发明新架构，只是换了动作对象。

---

## 四、CogAgent（清华 GLM）——视觉语言模型侧的升级

### ① 解决了什么问题
OS-Copilot / UFO 都是"框架/流程"层方案，但底层"**把截图看懂**"的能力才是真正的瓶颈。CogAgent 是一个 **180 亿参数的视觉语言模型（VLM）**，专为 GUI 场景训练：不仅能描述界面，还能**直接输出要点击的坐标**（高分辨率输入 + 交互式坐标感知）。

### ② 架构图（ASCII）

```
┌────────────────────────  CogAgent（清华 GLM 团队） ────────────────────────┐
│                                                                           │
│   GUI 截图（高分辨率 1120×1120 输入）                                        │
│        │                                                                  │
│        ▼                                                                  │
│  ┌─────────────────────────────────────┐                                  │
│  │   Vision Encoder（视觉编码器）        │                                  │
│  │   ── 把像素块转成图像 token           │                                  │
│  └───────────────┬─────────────────────┘                                  │
│                  ▼                                                        │
│  ┌─────────────────────────────────────┐                                  │
│  │   Language Model（GLM 语言模型）      │                                  │
│  │   ── 多模态对齐 + 界面语义理解         │                                  │
│  │   ── 输出：动作 + 坐标               │                                  │
│  └───────────────┬─────────────────────┘                                  │
│                  ▼                                                        │
│   输出: "点击(325, 480) — 登录按钮"  / 文本操作 / 说明                        │
│                                                                           │
│   ★ 创新点：不靠外部 OCR/坐标回归，模型原生理解"哪里可以点、点了干什么"          │
└───────────────────────────────────────────────────────────────────────────┘
```

### ③ 三要素如何体现
- **视觉理解（模型侧，核心）**：把"截图 → 界面语义 + 点击坐标"这件最难的事，从"框架里外挂工具"变成"**模型本身的能力**"。
- 动作空间 / 记忆：CogAgent 提供的是"**眼睛**"，动作执行仍靠外部框架（与 UFO / OS-Copilot 互补）。

### ④ 与你的 embedding / rerank 的连接（感知层升级的同一叙事）
| 你的代码 | 作用 | CogAgent 类比 |
|---------|------|--------------|
| [`embedding.py`](app/rag/embedding.py:1) | 文本 → 向量（机器能算的表示） | 像素 → 界面语义 + 坐标 |
| [`reranker.py`](app/rag/reranker.py:1) / Cross-Encoder | 精排：从粗召回里挑最相关 | （类比）从"看得到界面"到"看准该点哪" |

> **一句话：embedding/rerank 让 RAG 更懂文本，CogAgent 让 Agent 更懂屏幕——都是"感知层升级"：把机器看不懂的原始输入变成可推理的结构化表示。**

---

## 五、三篇论文的工程化分工（合起来看）

```
                    用户任务
                       │
                       ▼
     ┌──────────────────────────────────────────────┐
     │  Vision：CogAgent（视觉理解，模型侧）           │
     │  "看清楚屏幕：这是哪个按钮、坐标在哪"            │
     └──────────────────────────────────────────────┘
                       │ 界面语义 + 坐标
                       ▼
     ┌──────────────────────────────────────────────┐
     │  Decision：UFO AppAgent / FRIDAY 核心控制器    │
     │  "决定下一步做什么（决策 Agent）"               │
     └──────────────────────────────────────────────┘
                       │ 决策
                       ▼
     ┌──────────────────────────────────────────────┐
     │  Execution：UFO ActionAgent / FRIDAY 执行模块  │
     │  "具体怎么操作（执行 Agent）"                   │
     └──────────────────────────────────────────────┘
                       │ 执行结果 / 新截图
                       └──────────────→ 回 Vision（复盘）
                       │
                       ▼
     ┌──────────────────────────────────────────────┐
     │  Memory：FRIDAY 能力库（经验沉淀 → specialist） │
     │  "把这次经验固化成下次直接调用的能力"            │
     └──────────────────────────────────────────────┘
```

> **三篇论文正好是三要素的工程化落点：CogAgent 补视觉、UFO 补决策/执行分工、OS-Copilot 补记忆/能力演化。**

---

## 六、"Computer Use 想做好需要什么"清单（呼应 Week 7 Harness/安全）

| 要素 | 说明 | 呼应你已有的代码 |
|------|------|----------------|
| **① 视觉模型（看得准）** | CogAgent 这类 VLM 保证"认得出按钮 + 坐标"；视觉准确率直接决定后面所有步骤 | 类比 embedding 语义有误差 → 这里误差直接"点错地方" |
| **② 可靠动作执行（做得准）** | 动作空间设计（click/type/scroll）+ 执行后校验（点完确认界面是否变化） | 你的 `tools/call` 有明确返回；GUI 执行要"确认结果" |
| **③ 错误恢复 / self-correct（错了能回来）** | 操作失败 → 重试 / 回退 / 重新规划；不能"点一下就算成功" | [`reliability.py`](app/agent/reliability.py:1) 的异常重试 = Harness 可靠性的 GUI 版 |
| **④ 安全边界（不出事）** | 沙箱 / 权限限制 / HITL 审批门；Agent 在真实桌面，误删误点风险高 | [`research_agent_hitl.py`](app/agent/research_agent_hitl.py:1) 的 HITL 审批门（Week 7 安全三原则） |

> **一句话：光有"看得懂"不够，还得"点得准、错了能回、出事有边界"——这就是 Week 7 Harness 四个要素（Memory / 权限沙箱 / 任务调度 / 异常重试）在 GUI 场景的翻版。**

---

## 七、面试话术：用"UFO = 我的 Supervisor 模式的 GUI 版"聊前沿

**主线话术（回答"你怎么看 Computer Use / UFO 这类前沿"）：**

> "UFO 的双 Agent 架构，其实就是我 Supervisor 模式的 GUI 版：`AppAgent` 负责决策'下一步操作哪个 UI 元素'，`ActionAgent` 负责执行'怎么点'——和我 [`supervisor_agent.py`](app/agent/supervisor_agent.py:1) 里 `supervisor_node` 分发 + 子 Agent 干活是**同一套 Multi-Agent 思想**，只是执行对象从工具调用换成了屏幕元素。而 OS-Copilot 的能力库，就是我 Skill 机制（[`skill.py`](app/agent/skill.py:1)）的前沿进化——我做了注册→发现→调用，它多了从任务中**自举新能力**的闭环。所以 Computer Use 没有发明新架构，只是把'工具'换成了'屏幕'，把'决策/执行/记忆'这套我已有的框架复用了。"

**备用短句（30 秒）：**

> "Computer Use 的三篇代表工作——CogAgent 补视觉（模型看懂屏幕）、UFO 补决策/执行分工（双 Agent = 我的 Supervisor 的 GUI 版）、OS-Copilot 补记忆（能力库 = 我的 Skill 机制的进化）。核心难点还是 Harness 那套：视觉要准、执行要可靠、错了要 self-correct、出事要有安全边界。"

---

## 八、Day 2 任务自检

- [x] 逐一讲解三篇论文/项目（OS-Copilot / UFO / CogAgent），每篇按"① 解决什么问题 ② 架构图 ③ 三要素如何体现"三步
- [x] 画出至少两张 ASCII 架构图：UFO 双智能体 + OS-Copilot 能力库（另含 CogAgent 一张）
- [x] 连接用户已有知识：UFO 双 Agent ↔ Supervisor/Router；OS-Copilot 能力库 ↔ Skill 注册→发现→调用；CogAgent ↔ embedding/rerank
- [x] 提炼"Computer Use 想做好需要什么"：视觉模型 + 可靠动作执行 + 错误恢复（self-correct）+ 安全边界（呼应 Week 7 Harness）
- [x] 面试话术：能用"UFO 双 Agent = 我 Supervisor 模式的 GUI 版"来聊前沿

> 下一步（Day 3）：求职冲刺——把 8 周成果变成 `resume.md`（3 条 STAR 描述）。
> Computer Use 到此收口：概念（Day 1）+ 前沿论文反刍（Day 2）已完成，接下来是把整个 8 周串成一个面试故事。
